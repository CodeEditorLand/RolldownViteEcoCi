{
	"dependencies": {},
	"devDependencies": {
		"@preact/preset-vite": "2.9.0"
	},
	"name": "preact-ts",
	"scripts": {
		"build": "tsc -b && vite build",
		"dev": "vite",
		"preview": "vite preview"
	}
}
