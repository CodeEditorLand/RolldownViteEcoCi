{
	"dependencies": {},
	"devDependencies": {
		"@preact/preset-vite": "2.8.3"
	},
	"name": "preact-ts",
	"scripts": {
		"build": "tsc -b && vite build",
		"dev": "vite",
		"preview": "vite preview"
	}
}
